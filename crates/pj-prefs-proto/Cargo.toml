[package]
name = "pj-prefs-proto"
description = "Wire protocol between the freenet-pj app and its preferences delegate"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

# Minimal on purpose: this crate's contents decide the preferences delegate's code
# hash, and therefore where every stored preference lives. See the module docs.
[dependencies]
serde.workspace = true
bincode.workspace = true

[lints]
workspace = true
