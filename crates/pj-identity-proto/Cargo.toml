[package]
name = "pj-identity-proto"
description = "Wire protocol between the freenet-pj app and its identity delegate"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

# Dependencies are kept to the bare minimum on purpose: this crate's contents
# decide the identity delegate's code hash, and therefore where every user's key is
# stored. See the module docs.
[dependencies]
serde.workspace = true
bincode.workspace = true

[lints]
workspace = true
